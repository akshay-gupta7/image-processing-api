export function processImage(name: String, width: Number, height: Number){
    if(width==0 || width ==null || height ==0 || height == null){
        console.log("Make sure height and width are numbers greater than 0");
    }
}